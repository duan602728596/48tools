{
  "name": "@48tools/main",
  "version": "3.6.0",
  "description": "Electron主程序运行源代码",
  "license": "GPL-3.0",
  "scripts": {
    "start": "npm run clean && cross-env NODE_ENV=development tsc --watch",
    "dev": "npm run clean && cross-env NODE_ENV=development tsc",
    "build": "npm run clean && cross-env NODE_ENV=production gulp",
    "clean": "rimraf ./lib"
  },
  "dependencies": {
    "@electron/remote": "^1.0.4",
    "node-media-server": "^2.2.4"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-terser": "^2.0.1",
    "gulp-typescript": "^6.0.0-alpha.1"
  }
}
