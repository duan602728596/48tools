- var imports = require('./imports');

doctype html
html
  head
    meta(charset='utf-8')
    meta(name='renderer' content='webkit')
    meta(name='viewport' content='width=device-width, initial-scale=1')
    include components/Theme/stylesheet

  body.theme
    div.app#app
      include components/loading/loading

    include components/initialState/initialState

    script(src=require('./pages/PlayerWindow/Danmu/sdk/NIM_Web_SDK_v8.0.0'))
    if htmlWebpackPlugin.options.mode === 'development'
      script(src=imports.dll)